// src/features/warehouse/receiving/services/api.js

/**
 * درزِ تعویضِ mock و سرور برای دریافت انبار.
 *
 * `queries.js` و `mutations.js` فقط از همین فایل import می‌کنند، پس
 * مهاجرت به بک‌اند یعنی جابه‌جا کردنِ همین دو خط — نه گشتن در چند فایل
 * و نه دست‌زدن به هیچ هوک و کامپوننتی.
 *
 * دو ماژول عمداً سطحِ دقیقاً یکسانی دارند: همان نام‌ها، همان
 * آرگومان‌ها، همان شکلِ خروجی. اگر روزی یکی از دو طرف جلو افتاد، همین
 * فایل جای درستِ دیدنِ آن اختلاف است.
 */

export * from "./api-mockData";
// export * from "./api-v1";
