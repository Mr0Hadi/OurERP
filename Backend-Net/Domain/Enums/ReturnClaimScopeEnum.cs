namespace Domain.Enums
{
    /// <summary>Whether a claim is tied to an order line or not (matches frontend CLAIM_SCOPES).</summary>
    public enum ReturnClaimScopeEnum
    {
        ON_ORDER,
        OFF_ORDER,
    }
}
